export default function Applied() {
    return (
        <div className="flex grow flex-col items-center justify-around">
            <div>
                You already applied for the allowlist <br /> come back later to check if you have been added
            </div>

            {/* <p>refresh the page after completing deform?</p> */}
        </div>
    );
}
